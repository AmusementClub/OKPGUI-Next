# Controlled media fixtures

Placeholder for small fake media / path fixtures used by desktop WebDriver and packaged smoke.

- Prefer tiny generated files checked in later if needed for MediaInfo discovery
- Never use production user libraries or absolute home-directory authority paths
- Packaged smoke may use the **bundled** MediaInfo sidecar instead of live system MediaInfo
