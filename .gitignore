# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env (wiki paths, secrets) — never commit
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local AI/agent harness control state and instruction files.
# These are machine/session-owned and must never enter the repository.
.agents/
.claude/
.codex/
.continue/
.cursor/
.omc/
.omx/
.roo/
.windsurf/
.aider*
AGENTS.md
CLAUDE.md
GEMINI.md
.github/copilot-instructions.md
.github/instructions/

# Staged MediaInfo sidecars (scripts/stage-mediainfo.*); keep directory via .gitkeep.
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep

# Desktop E2E / packaged-smoke evidence output (local or CI artifact; not source)
tests/package-smoke/out/

# Playwright runtime output
test-results/

# pnpm content-addressable store (never commit)
.pnpm-store/
